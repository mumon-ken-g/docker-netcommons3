version: '3.5'
services:
  db:
    image: mysql:5.6
    volumes:
     - db_data:/var/lib/mysql
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: nc3
  web:
    build: ./php
    ports:
     - 80:80
    depends_on:
     - db
volumes:
  db_data:
